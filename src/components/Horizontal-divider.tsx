import {View} from 'react-native';
import * as React from 'react';

const HorizontalDivider = () => {
  return (
    <View style={{width: '100%', backgroundColor: '#eeeef0', height: 1}} />
  );
};

export default HorizontalDivider;
